import styled from "styled-components";

export const Button = styled.div`
    button {
        background-color: transparent;
        border: none;
        outline: none;
        color: white;
    }

    .item {
        width: 100%;
    }
`;